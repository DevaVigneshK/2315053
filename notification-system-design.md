# Notification System Design

frontend -> npm run dev

backend -> node server.js

frontend:
   create notification based on our wish and store on backend with effectively and filter through what we discover for specific url

backend:
    here,
    store,
    provide,
    logging
    to the server with log on every request and response on console 

loggin-middleware
    directly send through the backend server the log data so, remove the logging-middleware